machine:
  node:
    version: 0.12.0

dependencies:
  pre:
    - npm config set spin false
    - npm install -g npm@^2
    - npm install -g bower
    - npm install
    - bower install
  cache_directories:
    - 'node_modules'
